jdk:
  - openjdk17

# Chỉ publish các module library — bỏ qua :app (demo app) để tránh
# resource linking error làm fail toàn bộ build.
install:
  - ./gradlew :feedback:publishToMavenLocal :rate:publishToMavenLocal :privacy:publishToMavenLocal :exit:publishToMavenLocal -x test --no-daemon
